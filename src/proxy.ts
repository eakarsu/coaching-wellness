import{NextRequest,NextResponse}from'next/server';export function proxy(request:NextRequest){const p=request.nextUrl.pathname;if(p.startsWith('/api/v1/wellness'))return NextResponse.next();const localAuthEnabled=process.env.AUTH_MODE!=='oidc'&&process.env.BOOTSTRAP_ACKNOWLEDGEMENT==='create-initial-admin';if(localAuthEnabled&&(p==='/api/auth/login'||p==='/api/auth/me'))return NextResponse.next();const legacy=p.startsWith('/api/')||p.startsWith('/batch09/')||p.startsWith('/codex/')||['/admin','/register','/password-reset'].includes(p);if(legacy&&!(process.env.NODE_ENV!=='production'&&process.env.ENABLE_LEGACY_DEMOS==='1'))return NextResponse.json({error:'Legacy SQLite, weak-auth, generated-gap, and generic-AI surfaces are quarantined'},{status:404});if(['/clients','/fitness','/nutrition','/wellness','/appointments','/coach-match','/joint-plan','/recovery-readiness','/ai-insights','/profile'].includes(p))return NextResponse.redirect(new URL('/',request.url));return NextResponse.next();}export const config={matcher:['/api/:path*','/batch09/:path*','/codex/:path*','/admin','/register','/password-reset','/clients','/fitness','/nutrition','/wellness','/appointments','/coach-match','/joint-plan','/recovery-readiness','/ai-insights','/profile']};
